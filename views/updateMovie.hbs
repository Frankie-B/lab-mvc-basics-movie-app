<form action="/movie/update/{{movieHbs._id}}" method="POST"> 
    <input type="text" name="title" value="{{movieHbs.title}}">
    <input type="text" name="director" value="{{movieHbs.director}}">
    <input type="text" name="year" value="{{movieHbs.year}}">
    <input type="text" name="duration" value="{{movieHbs.duration}}">
    <button type="submit">Edit</button>
</form>
